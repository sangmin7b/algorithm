#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char const *argv[]){
    int N, M;
    cin >> N >> M;
    vector<int> mem, cost;

    for(int i=0;i<N;i++){
        int x; cin >> x;
        mem.push_back(x);
    }
    for(int i=0;i<M;i++){
        int x; cin >> x;
        cost.push_back(x);
    }

    

    return 0;
}
