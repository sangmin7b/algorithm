#include<bits/stdc++.h>
using namespace std;
using vi = vector<int>;
using vvi = vector<vi>;

int main(int argc, const char** argv) {
    int N, M, H;
    cin >> N >> M >> H;
    vvi bars(N - 1,vi(H,0));
 

    return 0;
}